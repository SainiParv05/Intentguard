# Demo Project

This is a sample internal tool used for inventory tracking. See `config.json`
for deployment configuration and `.env` for local environment variables.

Run `npm install` then `npm start` to launch locally.
